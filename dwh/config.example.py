"""Example configuration. Copy to `config.py` (do not commit secrets)."""

SOURCE_DATABASES = [
    {
        "name": "guarani_test_1",
        "conn_string": "postgresql://user:password@localhost:5432/guarani_test_1",
    },
]
